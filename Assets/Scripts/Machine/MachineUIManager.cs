using TMPro;
using UnityEngine;
using UnityEngine.UI;

public class MachineUIManager : MonoBehaviour
{
    [SerializeField] private TMP_Text m_MachineNameText;
    [SerializeField] private TMP_Text m_InputAmountText;
    [SerializeField] private TMP_Text m_OutputAmountText;
    [SerializeField] private Image m_InputImage;
    [SerializeField] private Image m_OutputImage;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
